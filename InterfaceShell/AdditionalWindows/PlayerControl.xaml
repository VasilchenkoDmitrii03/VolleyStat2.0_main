<UserControl x:Class="InterfaceShell.AdditionalWindows.PlayerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InterfaceShell.AdditionalWindows"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <StackPanel Orientation="Horizontal" >

        <TextBlock Margin="5">#</TextBlock>
        <TextBox x:Name="PlayerNumberTextBox" Width="50" Margin="5" Text="{Binding Number}" />
        <TextBlock Margin="5">Last name</TextBlock>
        <TextBox x:Name="LastNameTextBox" Width="100" Margin="5" Text="{Binding Surname}" />
        <TextBlock Margin="5">First name</TextBlock>

        <TextBox x:Name="FirstNameTextBox" Width="100" Margin="5" Text="{Binding Name}"/>
        <TextBlock Margin="5">Amplua</TextBlock>

        <ComboBox x:Name="RoleComboBox" Width="100" Margin="5" SelectedValue="{Binding Amplua}">
        </ComboBox>
        <TextBlock Margin="5">Height</TextBlock>

        <TextBox x:Name="HeightTextBox" Width="50" Margin="5" Text="{Binding Height}" />
    </StackPanel>
</UserControl>
