<UserControl x:Class="StatisticsCreatorModule.ScoreDisplayer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StatisticsCreatorModule"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="6*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="2*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Label Name="ScoreLabel" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Column="1"
      Background="{DynamicResource LabelBackgroundBrush}"
       Foreground="{DynamicResource TextBrush}"
       >0 : 0</Label>
            <Label Name="SetNumberLabel" FontSize="20" VerticalAlignment="Center">Set #1</Label>
        </Grid>
        
        <ListBox Name="ScoreListBox" Grid.Row="1"></ListBox>
    </Grid>
</UserControl>
